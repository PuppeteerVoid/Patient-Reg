import 'package:flutter/material.dart';

class SubsequentVisit extends StatelessWidget {
  const SubsequentVisit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('h'),),
    );
  }
}